<script>import Message from './Message.svelte';
export let field;
</script>

{#if field.validation && field.validation.errors.length}
	{#each field.validation.errors as error}
		<Message {error} messages={field.messages ? field.messages : []} />
	{/each}
{/if}
